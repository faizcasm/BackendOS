global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'backendos-monitor'

scrape_configs:
  - job_name: 'backendos-api'
    static_configs:
      - targets: ['api:3000']
    metrics_path: '/metrics'
    scrape_interval: 10s
